services:
  - type: static
    name: ksainv-frontend
    env: static
    root: client           # waar je package.json & render.yaml staan
    buildCommand: npm run build
    publishPath: dist      # wélke map je serveert *binnen* root
    routes:
      - type: rewrite
        src: /(.*)
        dest: /index.html
