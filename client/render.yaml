services:
  - type: static
    name: ksainv-frontend
    env: static
    root: client             # óf stel dit in via de Render-dashboard
    buildCommand: npm run build
    publishPath: dist        # ← Vite’s build-output
    routes:
      - type: rewrite
        src: /(.*)
        dest: /index.html
